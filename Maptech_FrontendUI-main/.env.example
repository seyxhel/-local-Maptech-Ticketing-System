# Example environment variables for the frontend nginx template
# Copy to `.env` and customize for your environment.

# Hostname or IP of the backend service (as reachable from the frontend container)
BACKEND_HOST=backend

# Port of the backend service
BACKEND_PORT=8000

# Optional: whether to use http or https when proxying (not currently used by template)
# BACKEND_SCHEME=http
